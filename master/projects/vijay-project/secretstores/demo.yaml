apiVersion: integrations.k8smgmt.io/v3
kind: SecretStore
metadata:
  name: demo
  project: vijay-project
spec:
  config:
    vault:
      clusters:
      - authPath: demo
        clusterName: vijay-mks-new
      host: https://www.google.com
  provider: Vault
