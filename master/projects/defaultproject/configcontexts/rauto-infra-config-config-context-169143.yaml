apiVersion: eaas.envmgmt.io/v1
kind: ConfigContext
metadata:
  name: rauto-infra-config-config-context-169143
  project: defaultproject
spec:
  envs:
  - key: INFRACOST_API_KEY
    sensitive: true
    value: sealed://INFRACOST_API_KEY
  secret:
    name: file://artifacts/rauto-infra-config-config-context-169143/sealed-secret.yaml
status: {}
