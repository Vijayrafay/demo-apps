apiVersion: eaas.envmgmt.io/v1
kind: ConfigContext
metadata:
  name: rauto-infra-config-config-context-752058
  project: defaultproject
spec:
  envs:
  - key: INFRACOST_API_KEY
    sensitive: true
    value: sealed://INFRACOST_API_KEY
  secret:
    name: file://artifacts/rauto-infra-config-config-context-752058/sealed-secret.yaml
status: {}
